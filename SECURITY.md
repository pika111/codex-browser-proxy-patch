# Security

This repository is intended to contain only generic tool code.

When sharing issue reports, avoid posting:

- local log files
- real home directory paths
- proxy credentials
- screenshots that include private browser state

Use placeholders such as `$HOME`, `<proxy-host>`, and `<proxy-port>` instead.
